<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/profile.css"> <!-- Your custom styles -->
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f6f7f8;
        }
        .profile-header {
            background-color: #0079d3;
            color: white;
            padding: 30px 0;
            text-align: center;
            position: relative;
        }
        .profile-header img {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            border: 4px solid white;
            position: absolute;
            bottom: -60px;
            left: 50%;
            transform: translateX(-50%);
        }
        .profile-content {
            margin-top: 60px;
        }
        .profile-info, .posts-section, .analytics-section {
            background-color: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }
        .profile-info h3, .posts-section h3, .analytics-section h3 {
            font-weight: bold;
            color: #333;
        }
        .post-item {
            padding: 15px;
            border-bottom: 1px solid #e9ecef;
        }
        .post-item:last-child {
            border-bottom: none;
        }
        .post-item:hover {
            background-color: #f8f9fa;
        }
        .post-title {
            font-size: 16px;
            font-weight: bold;
            color: #0079d3;
            text-decoration: none;
        }
        .post-title:hover {
            text-decoration: underline;
        }
        .post-meta {
            font-size: 14px;
            color: #777;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">thoughtVine</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive"
                aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarResponsive">
                <ul class="navbar-nav ml-auto">
                    {{#if currentUser}}
                    <li class="nav-item">
                        <a class="nav-link" href="/user/{{currentUser.username}}">Signed in as {{currentUser.username}}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout</a>
                    </li>
                    {{else}}
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/register">Sign Up</a>
                    </li>
                    {{/if}}
                </ul>
            </div>
        </div>
    </nav>

    <div class="profile-header">
        <img src="{{user.profilePicture}}" alt="{{user.username}}'s Profile Picture">
        <h2>{{user.username}}</h2>
        <p>{{user.emailId}}</p>
    </div>

    <div class="container profile-content">
        <div class="row">
            <!-- Left Sidebar (Profile Info) -->
            <div class="col-md-4">
                <div class="profile-info">
                    <h3>About</h3>
                    <p><strong>Username:</strong> {{user.username}}</p>
                    <p><strong>Email:</strong> {{user.emailId}}</p>
                    <p><strong>College:</strong> {{user.college}}</p>
                    <p><strong>Other Details:</strong> {{user.otherDetails}}</p>
                </div>
            </div>

            <!-- Main Content (Bio and Posts) -->
            <div class="col-md-8">
                <!-- User Bio -->
                <div class="analytics-section">
                    <h3>Bio</h3>
                    {{#if user.bio}}
                        <p>{{user.bio}}</p>
                    {{else}}
                        <p>No bio available.</p>
                    {{/if}}
                </div>

                <!-- Analytics Section -->
                <div class="analytics-section">
                    <h3>Analytics</h3>
                    <p><strong>Total Posts:</strong> {{user.posts.length}}</p>
                    <p><strong>Total Likes Received:</strong> {{user.totalLikes}}</p>
                    <p><strong>Total Comments Received:</strong> {{user.totalComments}}</p>
                </div>

                <!-- User Posts -->
                <div class="posts-section">
                    <h3>My Posts</h3>
                    {{#each user.posts}}
                    <div class="post-item">
                        <a href="/posts/{{this._id}}" class="post-title">{{this.title}}</a>
                        <div class="post-meta">
                            Posted {{this.createdAt}} | {{this.commentsCount}} comments | {{this.likesCount}} likes
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>

    <footer class="bg-dark text-white text-center py-3 mt-4">
        <div class="container">
            <p>&copy; 2024 thoughtVine. All Rights Reserved.</p>
        </div>
    </footer>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.bundle.min.js"></script>
</body>
</html>